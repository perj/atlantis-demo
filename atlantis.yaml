version: 3
projects:
  # Shared resources project - managed by Platform Atlantis
  - name: atlantis-shared
    dir: atlantis-servers/shared
    workspace: default
    autoplan:
      when_modified:
        - "*.tf"
        - "*.tfvars"
      enabled: true

  # Platform Atlantis server - managed by Platform Atlantis (self-managing)
  - name: platform-atlantis
    dir: atlantis-servers/environments/platform
    workspace: default
    autoplan:
      when_modified:
        - "*.tf"
        - "*.tfvars"
        - ../../modules/atlantis-server/**/*.tf
        - ../../modules/atlantis-server/templates/*.tpl
      enabled: true

  # System Alpha Atlantis server - managed by Platform Atlantis
  - name: system-alpha-atlantis
    dir: atlantis-servers/environments/system-alpha
    workspace: default
    autoplan:
      when_modified:
        - "*.tf"
        - "*.tfvars"
        - ../../modules/atlantis-server/**/*.tf
        - ../../modules/atlantis-server/templates/*.tpl
      enabled: true

  # System Beta Atlantis server - managed by Platform Atlantis
  - name: system-beta-atlantis
    dir: atlantis-servers/environments/system-beta
    workspace: default
    autoplan:
      when_modified:
        - "*.tf"
        - "*.tfvars"
        - ../../modules/atlantis-server/**/*.tf
        - ../../modules/atlantis-server/templates/*.tpl
      enabled: true
